[phases.setup]
aptPkgs = [
  "libgl1",
  "libglib2.0-0",
  "libsm6",
  "libxext6",
  "libxrender1",
  "tesseract-ocr",
  "poppler-utils"
]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"